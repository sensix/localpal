/* override bootstrap e varie */

.btn.focus, .btn:focus {
    outline: 0;
    box-shadow: 0 0 0 3px rgba(0,0,0,.25);
}


.carousel-indicators {
  bottom: -70px;
}

.carousel-indicators li {
  width: 15px;
  height: 15px;
  margin-right: 5px;
  margin-left: 5px;
  background-color: rgba(255,255,255,.5);
  border: 1px solid #fff;
  border-radius: 20px;
  cursor: pointer;
}

.accordion .btn {
  margin: 0.5rem 0;
  justify-content: left;
  background: #AC2828;
  color: #fff;
  font-weight: 700;
  font-size: 1.2rem;
  cursor: pointer;
  padding-left: 2rem
}

.convenzioni .btn {
  padding: 1px;
  margin: 0;
}

.convenzioni .btn img {
  line-height: 0;
}

.convenzioni .btn:hover img {
  opacity: 0.9;
}

.convenzioni img.dettaglio {
  width: 220px;
  margin: 10px;
  float: left;
}

.carousel-item {
  background-color: #444;
}

.carousel-item img {
  opacity: 0.6
}

.custom-control {
    position: relative;
    display: block;
    min-height: 1.5rem;
    padding-left: 1.5rem;
}

.custom-control-input {
    position: absolute;
    left: 0;
    z-index: -1;
    width: 1rem;
    height: 1.25rem;
    opacity: 0;
}

/* mbr */

a.text-white:hover, 
a.text-white:focus {
    color: #e73029 !important;
    text-shadow: 0px 0px 5px rgb(1,74,108);
}

.btn-white-outline, 
.btn-white-outline:active, 
.btn-white-outline.active {
  background-color: rgba(255,255,255,0.2);
}

/* varie*/

.navbar.bg-color a.text-white:hover,
.navbar.bg-color a.text-white:focus {
  color: #1F4E70 !important;
}